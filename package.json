{
  "name": "leslies-employee-portal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "concurrently -n api,frontend -c blue,green \"npm run dev --workspace=api\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=api && npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
