# Base URL of the separate API app (see ../api).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Virtual store number admins sign in with (must match the api's ADMIN_STORE_NUMBER). Defaults to 9999.
NEXT_PUBLIC_ADMIN_STORE_NUMBER=9999
